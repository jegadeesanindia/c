#include<stdio.h>
#include<conio.h>
int main()
{
 int a[100],n,i,j,k,m=0,b[100]={0},max;
 scanf("%d",&n);
 for(i=0;i<n;i++)
 {
  scanf("%d",&a[i]);
  }
  for(i=0;i<n;i++)
  {
    for(j=i+1;j<n;j++)
    {
      if(a[i]<a[j])
      {
	  m++;
      }
    }
    a[i]=m;
  }
  max=a[i];
  for(i=1;i<n;i++)
  {
    if(max<a[i])
    {
    max=a[i];
    }
  }
  for(i=0;i<n;i++)
  {
    if(max==a[i])
    { k=i;
    }
  }
  printf("%d",a[k]);
  for(i=k;i<n;i++)
  {
  for(j=i+1;j<n;j++)
  {
    if(a[i]<a[j])
    {
    printf("%d",a[j]);
    }
  }
  break;
  }
  getch();
  return 0;
}